[project]
name = "cronical"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "cron-descriptor>=2.0.8",
    "croniter>=6.2.2",
    "icalendar>=7.1.0",
    "python-crontab>=3.3.0",
    "python-dotenv>=1.2.2",
]
